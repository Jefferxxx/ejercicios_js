function areatriangulo(base, altura) {
    return (base * altura) / 2;
}

const baseTriangulo = 6;
const alturaTriangulo = 4;
alert,(`esta es el area del triángulo es: ${areatriangulo(baseTriangulo, alturaTriangulo)} unidades cuadradas`);